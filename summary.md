# Project Summary

## Current Status: Architectural Transition
The project is currently transitioning from a server-side rendered (Templ/HTMX/Tailwind) architecture to a high-performance, mobile-first stack using **SolidJS** and **Capacitor** (Android/Web). The Go backend is being refactored into a pure REST API.

## Bug Fixes
- **Tag filter input no longer loses focus**: Fixed a bug in `FilterEditor` where updating a tag filter caused the input field to lose focus by changing SolidJS `<For>` to `<Index>`, avoiding unnecessary DOM node recreation when array items are updated immutably.
- **Credit card debt no longer shows credit limit when balance is zero**: `DisplayBalance` for liability buckets (credit cards, loans) incorrectly fell back to `available_balance` when `balance` was 0. For credit cards, Plaid reports available credit (limit minus balance), so a paid-off card showed the full limit as debt. Liabilities now use only `balance` (amount owed); the available fallback remains for asset accounts only.
- **Google SSO no longer hijacks password accounts**: `AuthenticateViaGoogle` previously auto-linked a Google login to any existing account that shared the same email, silently signing the user in even if they had only ever set a password (and never set up SSO). Anyone who controlled that Google address could take over the account. It now refuses: when no `user_sso` row matches the Google account but an account with that email already exists, the service returns the new sentinel `auth.ErrSSOAccountConflict` instead of linking. The SSO callback (`handler/sso.go`) maps this to the redirect error code `sso_account_exists`, and the SSO complete page (`routes/auth/sso/complete.tsx`) shows "An account with this email already exists. Sign in with your password, then link Google from Settings." New accounts via Google (no existing email) still register and link normally; users who explicitly linked Google from Settings continue to sign in via SSO.
- **No flash of unrendered content during the login → onboarding/dashboard handoff**: The post-auth overlay (`auth-transition`) was dismissed too early when landing on onboarding, exposing a blank/partially-rendered frame while the wizard's first-step data (subscription resource) was still loading under the root `Suspense`. Fixes: (1) `routes/login.tsx` now navigates to `postAuthPath(profile)` so new users go straight to `/onboarding` instead of bouncing through `/dashboard`; (2) `routes/onboarding.tsx` holds the overlay until auth has resolved and `subscription.loading` is false before calling `endAuthTransition()` (removing the unconditional `onMount` dismissal); (3) `routes/auth/sso/complete.tsx` keeps the overlay up across the route change for the onboarding destination and lets the destination page release it. `auth-context.tsx`'s `refetch` is now typed to return the resolved `SessionProfile`.
- **Dashboard handoff shows the dashboard skeleton, not a transition overlay**: The full-screen post-auth overlay ("Opening your dashboard" / "You're all set") persisted over the dashboard route until data loaded and could appear unstyled. For every dashboard-bound transition, the route now preloads the dashboard chunk via the new `preloadDashboardRoute()` helper (`lib/auth-transition.ts`) so the dashboard mounts instantly and shows its existing `DashboardSkeletonGrid` (`LoadingCrossfade`) while it fetches — no overlay text. Applied in `routes/login.tsx` (login → dashboard, styled in-card success animation still plays) and `routes/onboarding.tsx` `handleFinish` (finishing onboarding → dashboard, dropped the "You're all set" overlay). The global overlay is now used only for the login/SSO → onboarding handoff (which has no skeleton of its own).
- **Production HTTPS (Caddy auto-TLS)**: Prod uses Caddy's built-in Let's Encrypt integration — set `SITE_HOST` (bare hostname), `API_PUBLIC_URL`, and `FRONTEND_URL` (`https://`) in `.env`; no Certbot container or manual cert init step. Certs persist in the `caddy_data` volume.
- **Startup env validation**: `main.go` now calls `config.ValidateRequiredEnv()` before opening the database. Core requirements: `ENCRYPTION_KEY` (32 bytes), Plaid credentials when `FINANCIAL_PROVIDER=plaid` (`PLAID_CLIENT_ID` + sandbox or production secret), and in production `API_PUBLIC_URL` plus full `MAIL_SMTP_*` / `MAIL_FROM` for password reset delivery. Stripe keys are not required when subscriptions are disabled.
- **Subscriptions toggle**: Added `SUBSCRIPTIONS_ENABLED` (default `true`). When `false`, Stripe billing/checkout is disabled, per-tier bank/API limits are not enforced (unlimited usage), skeleton dev tier switching is off, the Settings Plan tab and onboarding plan step are hidden, and `GET /api/v1/subscription` returns `subscriptions_enabled: false`.
- **Invite-only registration (subscriptions disabled)**: When `SUBSCRIPTIONS_ENABLED=false`, new email and Google SSO sign-ups require a single-use admin invite code. `GET /api/v1/auth/registration-config` exposes whether codes are required; `POST /api/v1/auth/register` accepts `registration_code`; Google OAuth state carries the code for register-page SSO. Admins listed in `REGISTRATION_ADMIN_EMAILS` can issue codes via `POST /api/v1/admin/registration-codes` or the Settings → Account **Invite codes** panel (in development, the seeded `test@test.com` / `test` user is always an admin); bootstrap/first deploy uses `make registration-code`. New `registration_codes` table (bcrypt-hashed, 48-hour TTL, single-use). Register page shows an invite callout and code field only when the gate is active; SSO complete page maps `invalid_registration_code` / `registration_code_required` errors. Home and Settings footers link to the GitHub repo.
- **Login "Server error" in development**: `test_schema.sql` (applied on every dev startup via `main.go`) had drifted out of sync with `schema.sql` and the Go queries — its `users` table was missing `subscription_tier`, `subscription_started_at`, `stripe_customer_id`, `stripe_subscription_id`, and `onboarding_completed`, and it lacked the `user_privileges`, `stripe_api_usage`, `stripe_fc_items`, `stripe_fc_account`, `plaid_api_usage`, and `password_reset_codes` tables (plus the `transactions.provider` column). `GetUserByEmail` therefore failed with `no such column: subscription_tier`, surfacing as the generic 500 `server_error` ("Server error, try again later") on `POST /api/v1/auth/login`, and the `test@test.com` dev seed silently failed for the same reason. Rewrote `test_schema.sql` to mirror `schema.sql` exactly (full DROP-then-recreate for a clean dev reset). Login now returns 200 and seeds `test@test.com` / `test`.

## Completed Milestones

### Core Backend & Services
- **Thin Handler Refactor**: Decoupled HTTP logic from business logic across all domains (Auth, Plaid, Tags, Transactions).
- **Service Layer**: Implemented robust services for `Auth`, `Plaid`, `Tags`, `Transactions`, `Settings`, and `Dashboard`.
- **Model Organization**: Refactored `internal/models/models.go` into modular files (`user.go`, `session.go`, `plaid.go`, `transaction.go`, `category.go`, `dashboard.go`, `page.go`) for improved maintainability.
- **Routing Refactor**: Isolated legacy Templ/HTMX routes into a dedicated `legacy()` method in `routes.go` and renamed the handlers package to `legacyHandler` to clearly distinguish legacy logic from the new JSON API.
- **Unified Validation**: Centralized input validation (Email, Password, Name) in the service layer.
- **Plaid Integration**: 
    - Cursor-based transaction synchronization.
    - Intelligent account reconciliation (soft-disconnection, visibility toggles).
    - Support for Plaid Update Mode and item health monitoring.
- **Smart Tagging Engine**: Rule-based auto-tagging with support for exact matches, regex, and amount-based filters. Default tags are now assigned distinct colors from the tag palette during initial seeding (e.g., Dining Out is Rose, Groceries is Emerald, Bills is Red).
- **Security**: Implemented session-based auth, re-authentication for high-privilege actions, and strict ownership checks.

### Database & Storage
- **SQLite Schema**: Optimized schema for users, sessions, Plaid items, accounts, transactions, and tags.
- **Foreign Key Enforcement**: Enabled SQLite foreign keys for automated data cleanup (ON DELETE CASCADE).
- **Multi-Auth Support**: Support for multiple SSO providers and account linking.

### Legacy UI (Templ/HTMX)
- **ViewPoint Dashboard**: Modular, customizable dashboard with Chart.js visualizations.
- **Advanced Transactions**: Hierarchical category filtering, bulk tagging, and responsive design.
- **Notification System**: Event-driven toast notifications using Alpine.js and HTMX triggers.
- **Theme Support**: Persistent light/dark/auto theme modes.

### Development Environment
- **Dockerization**: Containerized the Go backend and SolidStart frontend for a consistent development experience.
- **Hot-Reloading**: Integrated `air` for Go and standard Vite/Vinxi HMR for the frontend.
- **Volume Optimization**: Configured selective volume syncing to ensure code changes are reflected instantly while excluding heavy assets like `node_modules` and build artifacts from the sync loop.
- **Lightweight Images**: Leveraged Alpine-based images for both Node and Go to minimize resource footprint.

## New Architectural Direction (In Progress)
- **Frontend**: Shifting to **SolidJS** for fine-grained reactivity and maximum efficiency.
- **Mobile**: Using **Capacitor** to target **Android** and **Web** platforms.
    - Initialized Capacitor in the `frontend` directory.
    - Added the Android platform.
    - Updated `GEMINI.md` with mobile-specific development commands.
- **Styling**: Moving to **Vanilla CSS / CSS Modules** to ensure lightweight, touch-friendly mobile performance (removing TailwindCSS).
- **Security & Efficiency**: Prioritizing minimal re-renders, secure mobile storage, and optimized API interaction.
- **Unified Session Management**: Refactored the Go backend to manage session cookies directly via `Set-Cookie` headers, eliminating the need for the SolidStart frontend to proxy cookies. This simplifies authentication for both Web and Mobile (Capacitor).
- **REST API Standardization**: Removed the custom `{ success, data, error }` JSON envelope. The API now follows standard REST conventions, returning raw data with 2xx status codes and structured `APIError` objects with 4xx/5xx codes.
- **Direct Client-to-API Communication**: Updated the SolidStart frontend and Capacitor configurations to allow direct communication between the browser/mobile app and the Go API (using `credentials: "include"` and updated CORS settings).
- **Pure SPA/SSG Transition**: Migrated the SolidStart frontend to a pure Single Page Application (SSR: false) using Static Site Generation (SSG). This ensures compatibility with Capacitor and improves load performance by eliminating server-side hydration.
- **Responsive App Layout**: Implemented a unified `AppLayout` with a top navbar for desktop/tablet and a fixed bottom tab bar for mobile devices, optimized for touch-friendly navigation.
- **Client-Side Auth Flow**: Refactored authentication routes (Login, Register, Forgot Password) to use client-side state management and direct API calls, removing the need for SolidStart server actions.
- **Simplified SSO**: Refactored the Google OAuth flow to set the session cookie directly during the redirect callback, making the one-time exchange token optional and streamlining the login process.
- **Type Synchronization**: Standardized TypeScript interfaces to match Go DTOs for improved type safety across the boundary.
- **Lean Session API**: `GET /api/v1/auth/me` now returns a `SessionProfile` DTO (name, email, theme, onboarding status) instead of the full internal `User` model.
- **Dashboard REST API**: Added `GET /api/v1/dashboard` and `POST /api/v1/dashboard/layout` JSON endpoints with a lean `DashboardPayload` that omits internal IDs, Plaid identifiers, and other server-only fields.
- **SolidJS Dashboard**: Implemented a customizable widget grid in the new frontend with ApexCharts visualizations, SortableJS drag-and-drop customization, and mobile long-press edit mode. On drop, the grid does not call `setLayout` — it silently mutates each widget's `order` field to match Sortable's DOM result. Because no signal updates, Solid's `<For>` never reconciles on release, so there is no flicker, no `insertBefore` errors, and no chart remounts. `handleSave` reads `layout()` whose widget objects already carry the up-to-date orders; the createEffect that resets layout from server data is gated on `props.editMode()` so it cannot stomp the in-progress reorder. The **Spending Trend** chart has been updated from an area chart to a **bar chart** for better monthly comparison. Widget span (1 vs 2 columns) is driven by `WIDGET_META` and applied via CSS modules so Net Worth, This Month, Spending Trend, and Recent Transactions span two grid cells on tablet/desktop; mobile tiles size to their content instead of fixed row heights. Donut chart center totals use theme `--text` / `--text-muted` colors (not ApexCharts' default gray) for readable contrast across all themes. Tag donut widgets show a compact abbreviated total in the chart center (with dynamic font sizing), the full precise total in the widget header, and full `formatCurrency` values in slice tooltips. Net Worth, This Month, and Spending Trend metric tiles each use semantic accent colors (green cash, emerald savings, indigo investments, red credit, orange loans, blue accounts; amber spend, emerald income, blue runway) via a shared `--tile-tone` CSS variable pattern. Updated **ApexCharts tooltips** to correctly match the active theme across all palette options (Tokyo Night, Coffee, etc.) by mapping all dark-palette themes to the chart's dark mode and applying CSS overrides to force tooltip colors, borders, and shadows to use the theme's CSS variables.
- **Docker Dev Proxy Fix**: Corrected Vite `server.hmr`/`origin` config for Caddy reverse-proxy access, added a frontend entrypoint that runs `npm install` on container start (fixes stale `node_modules` volumes), and routed `/_build/*` explicitly through Caddy.
- **Dashboard Null-Safe Arrays**: API payload normalization now returns `[]` instead of JSON `null` for empty dashboard slices; frontend coalesces nullable arrays before rendering widgets.
- **Global Theme Application**: Added a root `ThemeProvider` that loads the user's theme preference on app start and listens for OS color-scheme changes; `login()` and `register()` reload the theme after establishing a session.
- **Dashboard UI Polish**: Replaced text-based icons with custom SVGs for the widget drag handle and visibility toggle, and improved button styling with smoother transitions and better centering.
- **Login Success Transition**: Added a phased sign-in handoff on the login page — animated checkmark overlay, accent bloom, and shell fade-out — before navigating to the dashboard so authentication feels intentional rather than an abrupt route swap.
- **Dashboard Loading Skeletons**: Replaced the plain "Loading dashboard..." text with staggered widget skeletons (`DashboardSkeletonGrid`) that mirror the default grid spans and widget shapes (hero metrics, charts, lists, and quick actions) while dashboard data loads.
- **Settings Account Skeleton**: Added `AccountPanelSkeleton` for the settings Account tab — profile fields, security rows, sign-out, and delete-account placeholders with staggered shimmer — while settings profile data loads; other tabs keep the generic loading message.
- **Smooth Dashboard & Settings Init Loading**: Added shared `useMinLoadingHold` and `LoadingCrossfade` so dashboard and settings crossfade from skeleton placeholders into content with a minimum 320ms hold (matching the transactions page pattern). Dashboard widgets now stagger in with `widgetReveal` animations; settings uses the account skeleton for all tabs during initial load and softens the panel entrance to opacity-only to avoid choppy layout jumps.
- **Settings panel content sizing**: Fixed settings panels (especially Connections) staying at the account-tab skeleton height when content is shorter. `LoadingCrossfade` now keeps only one layer in document flow at a time, and settings mounts only the active tab panel so the card shrinks to match its content.
- **Post-login loading handoff**: Added a global auth transition overlay that persists across the login-to-dashboard route change, prefetches dashboard data during the exit animation, and dismisses only once the dashboard is ready — eliminating the blank flash after sign-in.
- **New-user onboarding**: Added a 3-step wizard at `/onboarding` (Welcome → Choose plan → Connect bank) for users with `onboarding_completed = 0`. New password and Google SSO signups redirect here instead of the dashboard; `POST /api/v1/auth/onboarding/complete` marks the flow finished. `SessionProfile` now includes `onboarding_completed`; `AppLayout` guards incomplete users back to onboarding. Bank connection is optional (skip for later). Shared `PlanPicker` component is reused in Settings and onboarding.
- **Independent Dashboard Layouts**: Implemented support for separate desktop and mobile dashboard layouts. The backend stores both in a unified JSON structure, while the frontend dynamically switches layouts based on viewport size and allows per-device customization without affecting the other view.
- **Icon Organization**: Centralized all SVG icons into a dedicated `frontend/src/components/icons` directory as reusable SolidJS components. Updated the dashboard, main navigation (navbar and tab bar), and settings page to use these unified icon components, including a newly added `LogOutIcon`.
- **Layout Refactor**: Moved all layout components (`AppLayout`, `AuthLayout`) from `frontend/src/components` to a top-level `frontend/src/layouts` directory. This better separates global structural components from atomic UI components and follows common SolidStart/React architectural patterns. Updated all route imports to reflect the new paths.
- **Transaction Page Refactor**:
    - **Modular Component Refactor**: Decomposed the massive `TransactionsPage` into 8 logical, reusable components (`TransactionHeader`, `TransactionToolbar`, `FilterPanel`, `TransactionList`, `TransactionCard`, `Pagination`, `BulkActionBar`, `TransactionSkeletons`) in `frontend/src/components/transactions/`. This reduced the main route file size by ~60% and improved code readability.
    - **Optimized Reactivity**: Implemented `batch` updates and grouped signal changes for filters to prevent redundant API requests and ensure smoother UI transitions.
- **Stable Metadata**: Introduced a dedicated metadata resource for categories and tags, ensuring filter dropdowns remain stable and populated even while transaction results are loading.
- **Flicker-Free Updates**: 
    - Integrated `useTransition` for filter and pagination updates, keeping the current UI visible while loading new data to eliminate skeleton-induced flickering.
    - Refactored `Suspense` boundaries to isolate metadata loading from transaction data, ensuring filter controls remain stable and interactive during refreshes.
    - Leveraged `data.latest` for result counts and pagination, preventing them from disappearing or triggering `Suspense` when existing data is being updated.
    - Improved filter transition feedback by wrapping "Clear all filters" in `startTransition` and implementing a global loading progress bar.
    - Added a `visualLoading` state with a minimum 250ms duration to prevent flickering and provide clear visual feedback during transaction updates.
    - Added a **"Sync transactions"** button to the Dashboard Quick Actions widget and the Transactions page header, allowing users to manually trigger a Plaid sync and refresh transaction data instantly.
- **Capacitor/Mobile Fixes**: 
    - Adjusted the bulk action bar's sticky position on mobile to prevent overlapping with the Capacitor bottom tab bar.
    - Replaced the inline scrollable filter panel on mobile with a full bottom sheet (backdrop, drag handle, sticky header/footer, body scroll lock) so all filters are reachable without scrolling the page.
    - Fixed a critical positioning bug where the mobile filter sheet would appear at the bottom of the long transaction list (making it inaccessible) by implementing conditional rendering: the filter panel is rendered inline for desktop to maintain the layout, but wrapped in a SolidJS `Portal` for mobile. This decouples the mobile bottom sheet from the animated page container while preserving standard desktop behavior.
    - Added an active-filter count badge on the filter toggle and larger touch targets for filter inputs on small screens.
    - Improved date handling for mobile browser compatibility and enhanced checkbox touch targets and layout spacing for better mobile ergonomics.
- **Filter Robustness & Performance**:
    - **UTC Alignment**: Standardized on UTC for transaction date processing. Frontend now parses filter selections as UTC timestamps and formats transaction dates using the UTC timezone, resolving a bug where local timezone offsets would cause incorrect filtering or date shifting.
    - **Intelligent Input Debouncing**: Implemented a unified debouncing effect for search, date range, and amount filters. This prevents the "UI reset" issue where rapid keystrokes triggered overlapping API requests and loading skeletons, ensuring focus is maintained and the UI remains stable while the user is inputting values.
    - **Improved Clear Flow**: Refactored `clearFilters` to reset all debounced state, ensuring a clean and consistent UI state after clearing active filters.
    - **Min/Max Amount Filter (Sign-Aware Fix)**: Transactions are stored with Plaid/Stripe's sign convention (positive = expense, negative = income), but the UI displays the inverted amount. The min/max filter previously compared the raw signed amount, causing `max=1` to match every income/refund regardless of size and `min=1` to exclude all income. The backend now filters on `ABS(t.amount)` so min/max apply to transaction magnitude consistently for both expenses and income. The handler also rejects `NaN`/`Inf` and normalizes to absolute value, and the frontend filter inputs (`min="0"`) coerce input to non-negative finite numbers before debouncing/sending.
- **Settings REST API**: Added JSON endpoints for profile (`GET/PATCH /api/v1/settings`), password, theme, SSO unlink, and full Plaid connection management (`/api/v1/plaid/*`). External DTOs omit internal IDs while preserving row/account identifiers needed for Plaid Link flows.
- **Google Account Linking (API)**: Extended encrypted OAuth state to support `action=link`, allowing authenticated users to link Google SSO from the SolidJS settings page without creating a new session.
- **SolidJS Settings Page**: Implemented a tabbed settings experience (`Account`, `Connections`, `Appearance`) with profile editing, password management, Google link/unlink, theme selection, and Plaid connection management. The Connections tab mirrors legacy manage-page behavior: accounts must be disconnected through Plaid before they can be removed, with clear in-app guidance. The Account tab includes a dedicated logout section, and a delete-account flow with password or Google re-authentication, final confirmation, and permanent data removal via new JSON endpoints (`GET/POST /api/v1/settings/delete/*`).
- **Expanded Theme Palette**: Added selectable themes beyond light/dark/system — Tokyo Night, Coffee, Forest, Rose, Midnight, and Parchment — with shared frontend/backend validation via `theme-options.ts` and `models.IsValidThemePreference`.
- **Settings UI Refactor**: 
    - Moved the "Plan and Billing" section from the Account tab to its own dedicated **"Plan"** tab in the settings page.
    - Added a custom `CreditCardIcon` to the icon library for the new tab.
    - Improved visual structure by ensuring all settings panels use a consistent `panelInner` wrapper.
    - Redesigned the Plan tab billing summary with accent-tinted cards, usage meters, and plan tier pricing so usage fits the billing theme instead of the Connections warning callout.
- **Tags REST API**: Added JSON endpoints for tag and category management (`GET/POST/PUT/DELETE /api/v1/tags`, `GET /api/v1/tags/:id/filters`, `POST /api/v1/tags/:id/move`, `POST/PUT/DELETE /api/v1/categories`). Mutations return the refreshed `TagsPayload` to avoid extra client round-trips.
- **SolidJS Tags Page**: Implemented the manage tags experience with category cards, tag pills, inline category rename, create/edit tag modals (color picker, category selector, and auto-tagging filters with Save & Apply), new category modal, and delete-category flow (move to Misc, move to another category, or delete all tags). Merge categories was intentionally omitted. **Tag drag-and-drop between categories**: Tag chips on the tags page are HTML5-draggable. Picking a chip up highlights every other category card with an accent dashed border, and the hovered drop target solidifies its border with an accent glow and lift. Dropping on a different category calls `POST /api/v1/tags/:id/move`. The drop performs an optimistic update — the chip jumps to its new category instantly — and the server response is reconciled back into the store. The page backs the tags state with a Solid `createStore` + `reconcile({ key: "id", merge: true })` so unchanged categories keep their proxy identity across updates; `<For>` therefore does not remount cards on move/rename/create/delete, eliminating the `cardIn` entry-animation replay (no "grid reload" flash). On error the page re-fetches and surfaces a status banner. Single clicks on the chip name/X still edit/delete the tag — drags only start on press+move. Tag pills on desktop now use a fixed min-height and normalized line-height so every pill aligns consistently in the category grid. Fixed a bug where default tag colors would not render on the Dashboard or Transactions pages by ensuring palette keys (e.g., "emerald", "rose") are correctly translated to hex codes using the `tagColorHex` utility in all relevant components.
- **Security Audit & Hardening (Completed June 2026)**:
## Security Audit & Hardening (Completed June 2026)
- **OAuth State Hardening**: Implemented HMAC-signed `state` parameters in the SSO flow to prevent authentication-based CSRF and account linking attacks.
- **Data Encryption at Rest**: Added AES-GCM encryption for sensitive Plaid access tokens. Tokens are now encrypted before being stored in SQLite and decrypted only when needed for bank communication.
- **CSRF Protection**: Enabled global CSRF middleware using Echo's double-submit cookie pattern. The `_csrf` cookie (non-HttpOnly) is set on every response and the SPA reads it to send back as the `X-CSRF-Token` header on state-changing requests. A `GET /api/v1/csrf` endpoint primes the token on SPA startup. The CORS config includes `X-CSRF-Token` in allowed headers and trusted origins are configured for the frontend. Frontend `clientApiRequest()` automatically handles CSRF token attachment for POST/PUT/PATCH/DELETE methods.
- **Server Hardening**: 
    - Enabled standard security headers (X-Frame-Options, X-Content-Type-Options, etc.) via Echo's `Secure` middleware.
    - Enforced `Secure` and `HttpOnly` flags on all session cookies in production environments.
- **Rate Limiting**: Implemented memory-based rate limiting on authentication endpoints (Login, Register, Forgot Password) to mitigate brute-force and credential stuffing attacks, with a bypass configured for development environments.
- **Plaid Subscription Limits**: Added per-user Plaid quotas based on `users.subscription_tier` (`free`, `plus`, `premium`). Free tier defaults to 2 bank connections and 100 Plaid API calls per billing period. Usage resets on the user's signup date for free plans, or on `subscription_started_at` for paid plans (monthly anniversary). All outbound Plaid API calls are counted in `plaid_api_usage` keyed by billing `period_start`. Skeleton subscription service and `GET/POST /api/v1/subscription` endpoints support tier catalog and dev-only plan switching until Stripe is integrated (`stripe_customer_id` / `stripe_subscription_id` columns reserved). Settings Plan tab shows plan cards, usage, and reset date. Limit errors return `403` (`plaid_item_limit`) or `429` (`plaid_api_limit`).
- **External Model Boundaries**: Moved API payload conversion helpers out of `app/internal/models/external` into the Plaid, Tags, and Transactions service packages so the external models package only contains frontend-facing DTO structs.
- **Legacy Stack Removal**: Removed the Templ/HTMX frontend and `legacyHandler` package now that the SolidJS app covers the same features. The Go server is a pure REST API (`/api/v1/*` plus `/health`). Deleted `app/web/`, legacy OAuth config, redirect-based auth middleware, `PageData`/`Notification` models, `WidgetsForRender`, and unused `MergeCategories` helpers. `make build` no longer runs `templ generate`. Updated `GEMINI.md` to describe the current REST + SolidJS architecture and removed legacy transition notes.
- **Stripe Financial Connections (default bank provider)**: Added `FINANCIAL_PROVIDER` env switch (`stripe` default, `plaid` alternate). New `services/financial` facade and `services/stripefc` package using `stripe-go/v86`. Unified SPA routes under `/api/v1/connections/*` with Stripe.js `collectFinancialConnectionsAccounts` on the frontend (`lib/connections.ts`, `lib/stripe.ts`). New SQLite tables `stripe_fc_items`, `stripe_fc_account`, and `stripe_api_usage`; `transactions.provider` column scopes reads/writes to the active provider only. Plaid service and `/api/v1/plaid/*` routes preserved unchanged. Auth middleware stale sync now wires through the active provider. Documented in `GEMINI.md`.
- **Default provider set to Plaid**: `FINANCIAL_PROVIDER` now defaults to `plaid`; set `FINANCIAL_PROVIDER=stripe` to use Stripe Financial Connections. Updated `GEMINI.md`.
- **Plaid transaction webhooks**: Added `POST /api/v1/plaid/webhook` with JWT verification (`Plaid-Verification` header + `/webhook_verification_key/get`), CSRF exemption, and background handling for `TRANSACTIONS`/`SYNC_UPDATES_AVAILABLE` (item transaction sync) and `ITEM`/`ERROR` (connection status). Link/update tokens register the webhook URL from `PLAID_WEBHOOK_URL` or `{API_PUBLIC_URL}/api/v1/plaid/webhook`. Documented `PLAID_WEBHOOK_URL` in `GEMINI.md`.
- **Plaid sync limits**: Account and transaction sync no longer count against monthly Plaid API quotas. Manual sync is rate-limited to once per minute per user (`429` with `plaid_sync_rate_limit` / `connection_sync_rate_limit`); webhooks and background stale sync are exempt. Dashboard and Transactions pages show sync feedback via a page-level status banner (same pattern as Settings); Stripe FC limits unchanged. `PageStatusBanner` auto-dismisses after 30 seconds and includes a manual dismiss control.
- **Frontend cleanup**: Removed SolidStart boilerplate (`Counter`, `about` route). Consolidated bank connection client code onto unified `/api/v1/connections/*` routes — `plaid.ts` is now a thin Plaid Link SDK wrapper, provider-agnostic session helpers live in `connections.ts`, and Stripe.js-only logic remains in `stripe.ts`. Plaid and Stripe SDK modules are dynamically imported only when a user starts a bank connection flow (not on every page). Added shared `reportSyncError` helper; transactions page reuses `useMinLoadingHold` instead of duplicating min-loading logic.
- **Backend cleanup**: Removed legacy `/api/v1/plaid/*` management routes (webhook only). Deleted unused server-side formatters and Tailwind tag-chip helpers from the Templ era. Consolidated Plaid/Stripe connection payload builders into `services/connections`. Extracted shared session loading in auth middleware. `PlaidHandler` is webhook-only.
- **Type sync cleanup**: `sync_types.go` now exports only SPA-facing DTOs (`external` package plus shared dashboard/subscription structs), excludes internal models (`User`, `PlaidItem`, `Transaction`, etc.), removes the unused `ActionResult` type, and generates proper `Record<string, T>` types for map aliases like `DashboardAccountGroups`. Connection payload builders moved out of `models/external` into `services/connections` so the external package stays struct-only.
- **Transaction CSV export (ZIP, metered)**: Added a "Data" tab to the Settings page with a one-click export of every visible transaction together with each transaction's tags and tag categories. The endpoint `GET /api/v1/transactions/export` now returns a **ZIP archive** (`application/zip`, `Content-Disposition: attachment; filename="financial-tracker-transactions-YYYY-MM-DD.zip"`) containing a single CSV (same date-stamped name with `.csv`). The service `TransactionService.ExportTransactionsZip` wraps an in-memory `zip.Writer` around `writeTransactionsCSV` so headers only commit once the archive is fully built; failures don't ship a partial download. Each export **consumes one API call** from the user's monthly quota — the new `subscription.Service.ReserveAPICall` helper is the single source of truth for billing-period checks and increments. Plaid's `reservePlaidAPICall` now delegates to it (mapping `subscription.ErrAPILimitExceeded` back to `ErrPlaidAPILimitExceeded`), so bank syncs and CSV exports share one counter (`plaid_api_usage`). When the cap is hit the handler returns **HTTP 429** with code `export_api_limit`. Monthly API limits were lowered to **Free 20 / Plus 50 / Premium 100** in `TierCatalog` to make the new shared counter meaningful. CSV columns unchanged: `date, name, merchant_name, amount, currency_sign, pending, provider, provider_category, tags, tag_categories`; amounts are inverted on export (debits negative, credits positive) and `currency_sign` labels each row. Frontend `DataPanel` downloads the response as a blob and triggers a hidden anchor click (works in the Capacitor Android WebView), updates copy to mention the ZIP wrapper and the 1-API-call cost, and surfaces 429 / other errors through `PageStatusBanner`. New `DatabaseIcon`/`DownloadIcon` icons; `.exportCard` styles add an accent aura, dashed schema panel, and footer CTA. `TransactionService` now requires the subscription service via its constructor.
- **Password reset codes**: Added secure temporary-code password recovery. `POST /api/v1/auth/forgot-password` issues a bcrypt-hashed 6-digit code (15-minute TTL, 60s resend cooldown after the first resend, 5 attempt cap); `POST /api/v1/auth/verify-reset-code` validates the code before password entry; `POST /api/v1/auth/reset-password` sets the new password and invalidates all sessions. New `password_reset_codes` table and `services/mail` package (`DevSender` logs codes in development; `SMTPSender` ready for production via `MAIL_SMTP_*` env vars). Forgot-password page is a three-step wizard with a live expiry countdown on the code step, resend-code (first resend immediate, then 60s cooldown), and no back-navigation after verification. Unknown emails and SSO-only accounts receive the same generic success response without sending a code (enumeration-safe). Login shows a success banner after reset.
- **Tags drag-and-drop works on touch**: Replaced the HTML5 native drag wiring on tag chips (which never fires on mobile browsers) with a unified Pointer Events controller in `frontend/src/components/tags/useTagDrag.ts`. Drag activates after the pointer moves 8 px from the press point so taps still open the edit/delete buttons, then a cloned floating "ghost" chip follows the pointer (`position: fixed`, `pointer-events: none`). Drop targets are discovered by hit-testing under the pointer with `document.elementFromPoint` and walking up to the nearest `[data-category-id]` (each `CategoryCard` now exposes this attribute). Auto-scroll kicks in within 72 px of the viewport top/bottom for moving tags across long category lists, the synthetic `click` after a drop is swallowed so the underlying button doesn't fire, and `body { user-select/touch-action: none }` is applied for the duration of the drag. `.tagChip` got `touch-action: none` + `-webkit-touch-callout: none` so touches route to our pointer handlers instead of being interpreted as page scrolls or iOS callouts. Page-level drag state (`draggingTag`, `hoverCategoryId`) lives in the controller and is passed down to `CategoryCard`, which is now visual-only — no more local drag signals or HTML5 `dragenter/leave/over/drop` listeners.
- **Tags page status banner unified**: Replaced the tags page's bespoke inline status div with the shared `PageStatusBanner` component (auto-dismiss + manual close), matching Dashboard, Transactions, and Settings. Dropped the unused `.statusOk`/`.statusError` rules from `tags.module.css`.
- **Backend dead code cleanup**: Removed unreachable functions and types flagged by `golang.org/x/tools/cmd/deadcode` and `staticcheck`. Deleted unused service helpers `dashboard.NetWorthContribution`, `dashboard.BucketTotalFromSummary`, `dashboard.ParseLayoutJSON`, `dashboard.VisibleWidgets`, the redundant `DashboardService.SaveLayout` (superseded by `SaveDeviceLayout`), `financial.Facade.Name`, `TransactionService.GetRecentTransactions`, the `UpdateStripeFCAccountStatus` query, the unused `ssoExchangeRequest` handler type, and `SSOService.ParseAPIReturnTo` (its tests now call `ParseAPIState` directly). Simplified the awkward `for params != nil { ... break }` loop in `stripefc.ingestAccountTransactions` (SA4004). Consolidated the repeated `user_id` extraction pattern across 33 handler call sites into a single `requireUserID(c)` helper in `internal/server/handler/response.go`. Build, vet, deadcode, staticcheck (U1000), and tests all clean.
- **Public landing page**: Rebuilt `/` as a full marketing home page with sticky nav, asymmetric hero, animated CSS dashboard preview mock (`DashboardPreview`), feature cards, how-it-works steps, and a closing CTA band. Uses `home.module.css` (replacing `intro.module.css`). Unauthenticated visitors see the page; signed-in users are still redirected via `RedirectIfAuth`. Existing Playwright landing tests preserved (same eyebrow, headline, lede, CTAs, and feature card titles).
- **Stripe subscription billing**: Added `services/stripebilling` with Checkout (`POST /api/v1/subscription/checkout`), Customer Portal (`POST /api/v1/subscription/portal`), and billing webhooks (`POST /api/v1/stripe/webhook`). Production plan changes go through Stripe; dev skeleton tier switching remains when `ENV=development`. Configure `STRIPE_PRICE_PLUS`, `STRIPE_PRICE_PREMIUM`, and `STRIPE_WEBHOOK_SECRET` (sandbox variants in development). Settings Plan tab redirects to Checkout for upgrades and exposes a Manage billing portal button. Webhooks sync `subscription_tier`, `subscription_started_at`, and `stripe_subscription_id` from Stripe events.
- **Per-user subscription overrides**: Added `user_privileges` table plus `SUBSCRIPTION_OVERRIDES` / `SUBSCRIPTION_OVERRIDE_USER_IDS` env vars to grant unlimited bank/API quotas and automatic Stripe coupon application at checkout. Overrides can also be set directly in SQLite (`INSERT INTO user_privileges ...`). Effective limits surface in the Plan tab as Unlimited meters.
- **Production-aware startup**: `main.go` now branches on `ENV`: development applies `test_schema.sql` and seeds `test@test.com` only when missing; production uses idempotent `schema.sql`, skips test seeding, reads `PORT`/`DATABASE_PATH`, and treats a missing `.env` file as non-fatal. Shared `config.IsDevelopment()` centralizes environment checks across server, auth cookies, mail, and Stripe key selection.
- **Production Docker stack**: Added `docker-compose.prod.yml`, `Dockerfile.proxy.prod`, `app/Dockerfile.prod`, `Caddyfile.prod`, and `env.prod.example`. Production runs the compiled Go binary (no `air`) and serves the static SolidStart SSG build directly from Caddy (`file_server` on `/srv`); no Vite/Vinxi/nginx at runtime. SQLite persists on a `db_data` volume with `ENV=production`. Caddy serves HTTPS via Let's Encrypt using bare `SITE_HOST` (e.g. `yourdomain.com`) with `API_PUBLIC_URL` / `FRONTEND_URL` set to matching `https://` URLs. Use `make prod-build` / `make prod-up`.

### Testing & Quality Assurance
- **Playwright E2E Suite**: Implemented a comprehensive End-to-End testing suite using **Python Playwright**.
    - **Shared Auth State**: Configured a `session` scoped fixture that performs a single login and persists cookies/storage state, significantly reducing test execution time.
    - **Full Coverage**: 8 test files with 50+ tests across all application areas: Authentication (success/failure/validation/protected-redirects), Registration (structure/required fields), Forgot Password (structure/confirmation), Dashboard (layout/all widget types/customize button), Transactions (heading/search/filters/sync/empty state), Tags (categories/add-tag buttons/defaults), Settings (tabs/profile form/security/SSO/URL params), Landing page (CTA links/feature cards), and cross-page Navigation (navbar/in-page links).
    - **Configuration**: Standardized the environment with `pytest.ini` and a dedicated `e2e/` directory for better maintainability.
    - **Readiness Check Fix**: Replaced the single-page retry loop with a two-phase approach: (1) raw HTTP polling via `urllib` to verify server connectivity, (2) fresh Playwright browser context per attempt to avoid stale page state. Switched Docker networking from `--add-host=host.docker.internal:host-gateway` to `--network host` for reliable host access on Linux. Updated `Dockerfile` and `run_tests.sh` defaults from `host.docker.internal` to `localhost`.
- **Removed Auth Transition Popup Text**: Removed the hardcoded "Welcome back" and "Opening your dashboard" transition overlay text that would briefly appear when transitioning from the login page to the dashboard. The `transitionLabel` and `transitionHint` paragraphs were removed from `frontend/src/layouts/AuthLayout.tsx` and `frontend/src/components/AuthTransitionOverlay.tsx`. Cleaned up the unused `authTransitionCopy` import in `AuthTransitionOverlay.tsx`.
- **Fixed Invite Code Copy**: Fixed a bug where clicking "Copy" on a newly generated invite code would silently fail in non-secure contexts (such as HTTP development servers or Capacitor WebViews). Implemented a robust fallback mechanism using `document.execCommand('copy')` with an invisible temporary textarea when `navigator.clipboard` is unavailable or throws an error.
